# worker/

Celery worker entrypoints and task definitions land here starting Phase 7
(Rapid Apply) and Phase 8 (browser automation). Phase 1 ships the directory
structure only — the `backend` service handles all API/auth traffic for now.
